import type { Window } from "./Window";
export type Input = {
    /** Window object from `list_apps()` or `list_windows()` to drag in. */
    window: Window;
    /** Starting X coordinate in the window screenshot. */
    from_x: number;
    /** Starting Y coordinate in the window screenshot. */
    from_y: number;
    /** Ending X coordinate in the window screenshot. */
    to_x: number;
    /** Ending Y coordinate in the window screenshot. */
    to_y: number;
    /** Screenshot id from the latest `get_window_state()` response for coordinate drags. */
    screenshotId?: string;
};
export type Return = Promise<void>;
/** Drag from one window coordinate to another. */
export type Function = (input: Input) => Return;
